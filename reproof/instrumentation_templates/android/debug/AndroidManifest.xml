<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <receiver
            android:name="io.reproloop.autotrace.AutoExportReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="io.reproloop.EXPORT_CAPTURE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
