<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application android:allowBackup="false" android:label="Reproof Live" />
    <instrumentation
        android:name=".LiveInstrumentation"
        android:targetPackage="io.reproof.live"
        android:label="Reproof Live" />
</manifest>
